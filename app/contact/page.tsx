"use client"
import {FormEvent,useState} from "react";import {createClient} from "@/lib/supabase/client"
export default function Contact(){const [sent,setSent]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await createClient().from("contact_submissions").insert({name:f.get("name"),email:f.get("email"),message:f.get("message")});setSent(true)}return <main><div className="shell page-title"><div className="eyebrow">We are here to help</div><h1>Contact us</h1><p>Questions about a piece, delivery, or your order? Send us a note.</p></div><section className="section"><div className="shell">{sent?<p className="notice">Thanks — your message is on its way to our team.</p>:<form className="form" onSubmit={submit}><label>Name<input className="input" name="name" required/></label><label>Email<input className="input" name="email" type="email" required/></label><label>Message<textarea className="input" name="message" rows={6} required/></label><button className="btn btn-primary">Send message</button></form>}</div></section></main>}
