import Link from "next/link";import {createClient} from "@/lib/supabase/server"
export default async function Account(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return <main><div className="shell page-title"><div className="eyebrow">Northstar account</div><h1>Your account</h1><p>Sign in to view orders and manage your details.</p><Link className="btn btn-primary" href="/auth/login">Sign in</Link></div></main>;const {data:orders}=await supabase.from("orders").select("id,status,total_cents,created_at").order("created_at",{ascending:false});return <main><div className="shell page-title"><div className="eyebrow">Northstar account</div><h1>Welcome back</h1><p>{user.email}</p></div><section className="section"><div className="shell"><h2>Order history</h2>{!orders?.length?<p className="notice">Your orders will appear here after checkout.</p>:orders.map(o=><div className="form" key={o.id}><strong>Order {o.id.slice(0,8)}</strong><p>{o.status} · KSh {(o.total_cents/100).toLocaleString()}</p></div>)}</div></section></main>}
