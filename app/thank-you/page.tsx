import Link from "next/link";export default async function ThankYou({searchParams}:{searchParams:Promise<{order?:string}>}){const p=await searchParams;return <main><div className="shell page-title"><div className="eyebrow">Order received</div><h1>Thank you for shopping Northstar.</h1><p>Your order number is <strong>{p.order?.slice(0,8)}</strong>. We will be in touch with delivery details.</p><Link className="btn btn-primary" href="/shop">Continue shopping</Link></div></main>}
