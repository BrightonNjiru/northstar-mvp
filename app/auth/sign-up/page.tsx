"use client"
import {FormEvent,useState} from "react";import {createClient} from "@/lib/supabase/client";import Link from "next/link"
export default function SignUp(){const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();const {error}=await createClient().auth.signUp({email,password,options:{data:{full_name:name},emailRedirectTo:`${window.location.origin}/auth/callback`}});setMessage(error?"Please check your details and try again.":"Check your email to confirm your account.")}return <main><div className="shell page-title"><div className="eyebrow">Join Northstar</div><h1>Create account</h1></div><section className="section"><div className="shell"><form className="form" onSubmit={submit}><label>Full name<input className="input" required value={name} onChange={e=>setName(e.target.value)}/></label><label>Email<input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input className="input" type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label>{message&&<p className="notice">{message}</p>}<button className="btn btn-primary">Create account</button><p>Already registered? <Link href="/auth/login">Sign in</Link></p></form></div></section></main>}
